# V4 版本算法设计与优化报告

## 1. 背景与优化目标

本项目的核心任务是在 300 秒仿真时间内，调度 10 辆无人车在共享道路网络上完成“原料运输、加工生产、成品配送”这一供应链流程，并尽可能获得更高总分。评分函数可以概括为：

$$
Score = R_{order} + R_{drop} - P_{collision} - P_{overtime}
$$

其中，$R_{order}$ 表示订单完成收益，$R_{drop}$ 表示原料投递奖励，$P_{collision}$ 表示碰撞扣分，$P_{overtime}$ 表示订单超时扣分。

V1 到 V3 主要解决的是任务调度问题，包括目标占用、订单优先级、需求感知补料等。V3 的特点是吞吐量较高，车辆会积极取料、投料和送单，但由于多辆车共享道路和目标点，在高密度运行时容易出现以下问题：

- 多辆车同时到达同一个加工区或原料区，停在同一个节点附近，持续触发碰撞。
- 两辆车在窄路或同一路段相向行驶，距离过近时来不及避让。
- 某些车辆已经完成交互或失去任务价值，但仍停留在交通热点，阻塞后续车辆。
- 如果避碰策略过度保守，又会导致车辆频繁绕路、清场或等待，从而降低订单吞吐。

因此，V4 的优化目标不是单纯“减少碰撞”，而是在保持 V3 高任务吞吐的基础上，尽量降低最严重、最持续的碰撞扣分。换句话说，V4 要解决的是：

$$
\max \left(R_{order} + R_{drop} - P_{collision} - P_{overtime}\right)
$$

而不是只优化某一个单项指标。

## 2. V3 暴露出的主要问题

在批量测试和日志分析中，V3 的平均订单完成数较高，但碰撞扣分也很高。例如在种子集合 `[1, 2, 3, 100, 231, 235]` 上，V3 的平均表现为：

| Version | Avg Score | Orders | Value | Drop | Collision | Overtime |
|---|---:|---:|---:|---:|---:|---:|
| V3 | 1791.4 | 22.3 | 2555 | 1013 | 1679 | 97.8 |

可以看到，V3 每局平均能完成 22.3 单，任务吞吐很强，但平均碰撞扣分达到 1679 分。由于一次碰撞会产生固定扣分，并且碰撞车辆有约 1 秒冷却时间，如果两辆车长期停在同一个点，碰撞会以近似周期性的方式不断累积。

这种持续碰撞通常不是普通路径最短路导致的短暂擦碰，而是由以下场景造成的：

1. **目标点堆叠**：加工区、原料区、成品区都是图上的固定节点，多辆车同时被调度到同一个节点后，很容易停在完全相同的位置。
2. **交互后滞留**：车辆完成 pick/drop 后，下一次任务分配可能仍然把它留在热点附近，或者新的车辆已经进入同一目标点。
3. **避让不及时**：如果只在距离较远时重规划，实际到达碰撞半径内后可能已经来不及错开。
4. **过度避碰的副作用**：早期 V4 曾尝试对所有近距离车辆做拥堵路径和目标占用控制，但结果表明这会显著降低订单完成数，得分反而下降。

因此，最终 V4 采用了“最小侵入式避碰”的设计原则：尽量不干扰 V3 的任务调度和高吞吐路线，只在明确进入危险状态时进行局部修正。

## 3. V4 的总体设计思想

V4 的核心思想可以总结为：

> 保留 V3 的任务调度作为主策略，在车辆即将或已经发生碰撞时，插入局部避让动作，让低价值或空闲车辆主动清场。

从控制结构上看，V4 可以分为两层：

1. **主任务层**：沿用 V3 的订单排序、目标 claim、需求感知补料等机制，决定车辆“应该做什么”。
2. **局部避碰层**：在每个 tick 后处理车辆间距离，决定是否需要覆盖某些车辆的原命令，使其临时清场或降速。

这种结构的好处是，V4 不重新设计整个调度系统，而是作为一个安全修正层叠加在 V3 上。形式上可以理解为：

$$
Command_{V4}(v) =
\begin{cases}
Override_{avoid}(v), & \text{if } v \text{ is in an emergency conflict} \\\\
Command_{V3}(v), & \text{otherwise}
\end{cases}
$$

也就是说，只有当车辆进入明确的危险状态时，V4 才覆盖 V3 的原始命令。

## 4. 关键策略一：确定性重规划与可控随机性

早期版本中曾保留一定概率的随机重规划：

$$
P_{random} = 0.15
$$

随机重规划的优点是有机会跳出局部拥堵，但缺点是批测波动较大，不利于课程评测和复现实验。最终 V4 将随机性改为默认关闭，只有设置：

```bash
STRATEGY_RANDOM_REPLAN=1
```

时才启用随机选择。

默认情况下，V4 采用确定性选择规则：

1. 优先保留收益更高的车辆。
2. 优先保留正在执行高价值成品任务或携带成品的车辆。
3. 优先让低收益、空车或低价值原料车避让。
4. 若收益接近，则按车辆编号稳定打破平局。

车辆任务价值用一个简单的启发式函数估计：

$$
Gain(v) = Reward(task_v) - \lambda \cdot Dist(v, target_v)
$$

其中，$\lambda = 0.03$，表示距离成本权重。任务收益按任务类型估计：

- 成品相关任务：使用对应订单产品价值，例如 B1 为 150，B5 为 90。
- 原料相关任务：默认收益估计为 30。
- 无活动任务车辆：收益为 0。

这个估计不追求全局最优，只用于局部冲突时判断“谁更值得保留当前任务”。

## 5. 关键策略二：同点紧急分离

V4 最重要的改动是“同点紧急分离”。碰撞判定由服务端完成，车辆半径为 1 米，两车距离小于 2 米时发生碰撞。因此 V4 将危险距离设为：

$$
d(v_i, v_j) < 2.0
$$

当两辆车进入该距离阈值时，V4 不再依赖普通路径重规划，而是立即触发 emergency separation。

### 5.1 冲突组识别

V4 会在每个 tick 检查所有车辆对：

$$
\forall (v_i, v_j),\quad d_{ij} = \lVert p_i - p_j \rVert_2
$$

若 $d_{ij} < 2.0$，则认为这两辆车属于同一个危险冲突组。如果多个车辆两两相连，则合并为一个冲突组：

$$
G = \{v_1, v_2, ..., v_k\}
$$

这样可以处理三辆或更多车辆同时堆在同一目标点的情况。

### 5.2 保留车辆选择

对于每个冲突组，V4 只保留一辆车继续原任务，记为 keeper，其余车辆执行临时清场动作。keeper 的选择依据包括：

- 当前任务预期收益 $Gain(v)$。
- 是否正在移动。
- 是否携带成品或原料。
- 是否执行成品投递、成品取货等高价值任务。

可以抽象为：

$$
Keeper = \arg\max_{v \in G}
\left(Gain(v) + B_{status}(v) + B_{cargo}(v) + B_{task}(v)\right)
$$

其中：

- $B_{status}$：移动车辆有额外加分，避免高价值在途任务被频繁打断。
- $B_{cargo}$：携带物品车辆优先级高于空车，携带成品优先级高于携带原料。
- $B_{task}$：成品相关任务额外加分。

### 5.3 清场车辆选择安全邻居节点

被清场车辆不会重新选择完整任务，而是临时移动到当前节点的一个安全邻居节点。安全邻居的选择原则是：

1. 不能是其它车辆当前占用的节点。
2. 不能是本 tick 已经为其它清场车辆预留的节点。
3. 尽量选择离其它车辆最远的邻居。

若当前节点为 $n$，其邻接节点集合为 $Adj(n)$，则清场目标可以表示为：

$$
n^* =
\arg\max_{u \in Adj(n)}
\min_{v_j \neq v_i} \lVert pos(u) - p_j \rVert_2
$$

清场命令的 action 设置为 `None`，仅发送短路径和速度：

```python
path = [current_node, safe_neighbor_node]
action = None
speed = CLEARANCE_SPEED
```

这样做的含义是：车辆只是临时让出位置，不改变原服务端协议，也不主动执行 pick/drop。

## 6. 关键策略三：近距离预警下的低收益车辆重规划

除了真实碰撞半径内的紧急分离，V4 还保留一个较小的预警半径：

$$
d(v_i, v_j) \leq 3.5
$$

当两辆车距离较近但尚未进入 2 米碰撞半径时，V4 会尝试对其中一辆车做局部重规划或降速。选择重规划车辆时，仍然使用前面定义的预期收益函数，优先让低收益车辆让路。

普通重规划遵循以下逻辑：

1. 只考虑正在 moving 且存在 active task 的车辆。
2. 若车辆刚刚被重规划过，则进入 cooldown，避免反复改路径。
3. 对于对向或同路段冲突，尝试将对方车辆的下一节点或未来若干节点加入 blocked set。
4. 调用带禁用节点的 Dijkstra 规划替代路径。
5. 若找不到替代路径，则降速错峰，而不是强制绕远路。

重规划 cooldown 最终调为：

$$
T_{cooldown} = 1.0s
$$

这个参数经过对比后比 2.0s 更适合当前地图。2.0s 时车辆可能在热点处停留太久，清场响应偏慢；1.0s 能更快解除持续碰撞，同时不会像过短 cooldown 那样造成过多抖动。

## 7. 关键策略四：保留但默认关闭的拥堵路径与目标占用

在调试过程中，我们还尝试了两个更激进的策略。

### 7.1 拥堵感知路径

拥堵感知路径会在普通任务寻路时，对其它车辆当前节点、下一节点和未来路径节点增加惩罚：

$$
Cost'(u, v) = Cost(u, v) + Penalty(v)
$$

使用过的主要惩罚参数包括：

| 参数 | 含义 | 数值 |
|---|---|---:|
| `CONGESTION_CURRENT_PENALTY` | 其它车辆当前节点惩罚 | 60 |
| `CONGESTION_NEXT_PENALTY` | 其它车辆下一节点惩罚 | 35 |
| `CONGESTION_FUTURE_PENALTY` | 未来路径节点惩罚 | 18 |
| `CONGESTION_TARGET_PENALTY` | 已有任务目标节点惩罚 | 25 |

这个策略理论上可以减少路上相遇，但实际测试发现它会让大量车辆绕路，降低订单吞吐。在当前任务中，订单收益远高于一次短暂绕行收益，因此最终将其默认关闭，仅保留环境变量开关：

```bash
V4_CONGESTION_PATH=1
```

### 7.2 物理目标占用

另一个尝试是：若加工区附近已经有 idle 车辆，则暂不派其它车辆前往同一加工区。这个策略可以减少目标点堆叠，但如果限制过严，会导致原料投递和成品生产变慢。早期测试中曾出现订单数大幅下降的情况。

因此最终也将其默认关闭，仅作为可选调试开关：

```bash
V4_PHYSICAL_ZONE_OCCUPANCY=1
```

这两个策略的调试结论是：在本项目评分结构下，不能为了减少碰撞过度牺牲吞吐。最终 V4 的有效版本是“只处理明确危险，不大范围重写路径”。

## 8. 参数设置与调优结论

最终 V4 默认使用的关键参数如下：

| 参数 | 数值 | 作用 |
|---|---:|---|
| `IDLE_CLEAR_DISTANCE` | 2.0 | 真实危险距离，低于该距离触发紧急分离 |
| `COLLISION_WARN_DISTANCE` | 3.5 | 近距离预警半径，用于普通重规划或降速 |
| `CLEARANCE_SPEED` | 12.0 | 清场移动速度 |
| `REPLAN_COOLDOWN_SECONDS` | 1.0 | 普通重规划冷却时间 |
| `EXPECTED_GAIN_WEIGHT` | 0.03 | 任务距离成本权重 |
| `RANDOM_REPLAN_PROB` | 0.15 | 随机重规划概率，仅在开启随机模式时生效 |

调参过程中的主要发现：

1. `CLEARANCE_SPEED=8` 清场偏慢，车辆容易继续在碰撞半径内停留。
2. `CLEARANCE_SPEED=16` 对部分 seed 有帮助，但在另一些 seed 上会造成新的路段冲突，稳定性不如 12。
3. `REPLAN_COOLDOWN_SECONDS=2.0` 响应偏慢，某些同点碰撞会持续较久。
4. `REPLAN_COOLDOWN_SECONDS=1.0` 在多 seed 上更稳定，能更快解除持续碰撞。
5. 大范围拥堵路径虽然降低局部冲突概率，但会明显影响订单吞吐，因此默认关闭。

## 9. 实验结果对比

根据后续复测要求，本节重新使用种子 `[123, 234, 345, 456, 567, 678]` 进行批量测试，并将仿真加速倍率设置为 `speed=100`。选择较低加速倍率的原因是：当 `speed` 过高时，服务端与策略端之间的状态更新、命令发送和碰撞检测更容易受到执行时序影响，可能放大偶然波动。因此，本组结果更适合作为稳定对比。

批测命令形式如下：

```bash
STRATEGY_LOG=0 python batch_test.py student_v4 123 234 345 456 567 678
```

其中 `batch_test.py` 中的 `SPEED` 设置为 `100`。

### 9.1 汇总结果

| Version | Avg Score | Orders | Value | Drop | Collision | Overtime |
|---|---:|---:|---:|---:|---:|---:|
| V3 | 2208.5 | 24.7 | 2796 | 1098 | 1624 | 61.5 |
| V4 final | 2926.7 | 23.7 | 2668 | 1063 | 732 | 71.6 |
| V5 | 1727.5 | 20.0 | 2261 | 938 | 1352 | 120.0 |

### 9.2 分种子结果

| Seed | V3 Score | V4 Score | V5 Score | V4 相对 V3 |
|---:|---:|---:|---:|---:|
| 123 | 1808.0 | 2825.5 | 1653.3 | +1017.5 |
| 234 | 2462.6 | 3072.8 | 1397.3 | +610.2 |
| 345 | 481.0 | 2123.9 | 554.1 | +1642.9 |
| 456 | 2071.2 | 2807.7 | 2087.7 | +736.5 |
| 567 | 2913.4 | 3627.2 | 2401.8 | +713.8 |
| 678 | 3514.9 | 3103.2 | 2270.5 | -411.7 |

从新的测试结果可以看到，V4 在 6 个种子中的 5 个种子上高于 V3，尤其在 seed=345 上提升最明显。该 seed 下 V3 的碰撞扣分达到 2425，导致虽然完成了 19 单，但总分只有 481.0；V4 将碰撞扣分压到 640，最终得分提升到 2123.9。这说明 V4 的主要价值不在于大幅增加订单数，而在于避免某些随机场景中出现灾难性的持续碰撞。

### 9.3 指标分析

从平均指标看，V4 相比 V3 的变化为：

$$
\Delta Score = 2926.7 - 2208.5 = 718.2
$$

平均分提升约 718 分。具体拆分如下：

$$
\Delta R_{order} = 2668 - 2796 = -128
$$

$$
\Delta R_{drop} = 1063 - 1098 = -35
$$

$$
\Delta P_{collision} = 732 - 1624 = -892
$$

$$
\Delta P_{overtime} = 71.6 - 61.5 = +10.1
$$

也就是说，V4 相比 V3 的订单收益和投料收益略有下降，说明避碰确实牺牲了一部分任务吞吐；但碰撞扣分平均减少约 892 分，这一项收益远大于吞吐损失。因此总体得分提升。

可以用评分函数解释这一点：

$$
\Delta Score
= \Delta R_{order}
+ \Delta R_{drop}
- \Delta P_{collision}
- \Delta P_{overtime}
$$

代入平均值近似为：

$$
\Delta Score
\approx (-128) + (-35) - (-892) - (10.1)
\approx 718.9
$$

这与实际平均分提升 718.2 基本一致。

### 9.4 与 V5 的比较

V5 在设计上尝试加入时空冲突检测和速度错峰，但在这组测试中平均分只有 1727.5，低于 V3 和 V4。主要原因是：

- V5 的平均碰撞扣分为 1352，虽然低于 V3 的 1624，但明显高于 V4 的 732。
- V5 的平均订单数为 20.0，低于 V3 的 24.7 和 V4 的 23.7。
- V5 同时损失了吞吐，又没有像 V4 那样显著压低碰撞，因此总分最低。

这进一步说明，复杂的路径协同机制不一定带来更好的总分。当前地图和评分规则下，最有效的是 V4 这种“只在真实危险出现时插手”的局部安全修正，而不是对所有路径进行较大范围的时空规划。

### 9.5 结论

在 `speed=100`、种子 `[123, 234, 345, 456, 567, 678]` 的复测条件下，V4 的平均总分最高：

$$
Score_{V4}=2926.7 > Score_{V3}=2208.5 > Score_{V5}=1727.5
$$

因此，V4 的优化方向是有效的：它保留了 V3 的主要吞吐能力，同时显著降低持续碰撞带来的扣分。虽然 V4 的订单数略低于 V3，但由于碰撞惩罚下降幅度更大，最终总分更高。

## 10. 总结

V4 的核心贡献是把“路径协同”从全局重规划问题转化为局部安全修正问题。它没有推翻 V3 已经有效的任务调度，而是在车辆进入真实危险区域时插入一层轻量级避碰控制。

最终 V4 的设计可以概括为三点：

1. **高吞吐优先**：默认保留 V3 的任务分配与最短路导航，不轻易打断车辆任务。
2. **真实危险才干预**：只有车距小于 2 米时，才触发紧急同点分离。
3. **低价值车辆让路**：用任务收益估计选择 keeper，让空车或低收益车清场，高价值车辆继续执行任务。

这个设计体现了多智能体协同运输中的一个重要经验：避碰策略不能脱离评分函数单独优化。对于本项目，最优方向不是让所有车辆走得更“安全保守”，而是在尽量维持任务流转速度的同时，消除最昂贵、最持续的碰撞源。
