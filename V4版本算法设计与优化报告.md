# V4 版本算法设计与优化报告

## 1. 优化目标

本项目总分为：

$$
Score = R_{order} + R_{drop} - P_{collision} - P_{overtime}
$$

V3 的任务吞吐较强，但在目标点和窄路节点容易出现持续碰撞。V4 的目标是在不允许车辆穿越草坪、严格沿 `map.json` 道路边行驶的前提下，通过低扰动合法避让降低碰撞扣分，同时尽量保持 V3 的订单吞吐。

本轮固定验收种子为：

```text
100, 252, 216, 628, 987
```

额外随机泛化种子为：

```text
37, 149, 314, 512, 777, 901
```

## 2. 道路合法性约束

可通行道路以 `map.json` 的 `edges` 为唯一依据。服务端会把这些边加载为 `state.adjacency`，SDK 侧也会把同一批边加载为 `_adjacency`。因此，V4 生成的路径必须满足：

$$
(n_i,n_{i+1}) \in E
$$

其中 $E$ 为地图边集合。

此前可视化中出现穿草坪，原因是车辆位于道路中点时，重规划路径直接从“最近节点”开始，服务端会按 path 中的直线段移动，导致车辆切向另一个节点。修复后，V4 所有覆盖路径都使用合法锚点：

$$
Path =
[p_{current}, p_{anchor}]
\oplus
Dijkstra(n_{anchor}, n_{target}; E)
$$

也就是说，moving 车辆必须先继续走到当前 `path_preview[0]` 对应的道路节点，再从该节点沿地图边重新规划。除首段当前位置到锚点外，后续节点对都必须存在于 `_adjacency`。

## 3. 日志复盘发现

本轮没有只看宏观分数，而是对低分 seed=100 开启日志复盘。旧 V4 合法避让下：

- `v4_replan` 全部为 `idle_clearance`，没有默认 moving 绕路。
- seed=100 中清场事件约 178 次，过于频繁。
- 高频清场车辆集中在 `v10`、`v8`、`v2`、`v3`。
- 高频节点对包括 `n23 -> n44`、`n44 -> n23`、`n39 -> n40`、`n40 -> n39`。

这说明问题不是“绕路不够多”，而是清场车辆在热点节点之间来回摆动：

$$
n_a \rightarrow n_b \rightarrow n_a \rightarrow \cdots
$$

这种振荡虽然降低了一部分碰撞，但会反复打断任务节奏，导致订单吞吐下降。

## 4. 最终 V4 策略

最终采用“合法 idle 清场 + 清场防振荡”的低扰动策略。

### 4.1 默认开启合法避让

V4 默认开启合法避让：

```bash
V4_AVOIDANCE=1
```

但默认关闭普通 moving 重规划：

```bash
V4_MOVING_REPLAN=0
```

原因是测试发现无条件 moving 重规划会降低部分高吞吐 seed 的订单数。moving 重规划代码仍保留，但只作为可选实验能力。

### 4.2 idle 清场

当两车距离进入危险半径：

$$
d(v_i,v_j) < 2.0
$$

若存在 idle 车辆，则选择低收益 idle 车辆移动到合法邻接节点清场。清场目标从当前锚点的邻接节点中选择，避开：

- 其它车辆当前节点；
- 本 tick 已预留清场节点；
- 活动任务目标节点；
- 近期热点节点会被降权。

清场命令不携带 pick/drop action：

```python
action = None
```

这样车辆只是让出位置，不改变通信协议。

### 4.3 清场防振荡

日志显示的主要问题是同一车辆被频繁清场，因此加入清场冷却：

$$
T_{clearance} = 2.5s
$$

同一车辆在冷却期间不再被清场。清场目标也会写入 `reserved_clearance_nodes`，避免同一 tick 多辆车选择同一个邻居点。

### 4.4 保留但默认关闭的能力

以下能力保留为可选开关，但不是最终默认评分策略：

```bash
V4_MOVING_REPLAN=1
V4_TARGET_STAGING=1
V4_CONGESTION_PATH=1
```

它们在部分 seed 上有收益，但会牺牲其它 seed 的吞吐稳定性，因此本轮不作为默认。

## 5. 关键参数

| 参数 | 默认值 | 含义 |
|---|---:|---|
| `COLLISION_WARN_DISTANCE` | 3.5 | 近距离检测半径 |
| `IDLE_CLEAR_DISTANCE` | 2.0 | 危险清场半径 |
| `CLEARANCE_SPEED` | 12.0 | 清场速度 |
| `CLEARANCE_COOLDOWN_SECONDS` | 2.5 | 同车清场冷却 |
| `REPLAN_COOLDOWN_SECONDS` | 1.0 | 可选重规划冷却 |
| `V4_AVOIDANCE` | 1 | 默认启用合法 idle 避让 |
| `V4_MOVING_REPLAN` | 0 | 默认关闭 moving 绕路 |

额外测试 `CLEARANCE_SPEED=14` 后，固定 seed 平均分为 `2407.9`，低于默认速度 12 的 `2508.1`，因此不采用。

## 6. 实验结果

批测均使用：

```bash
STRATEGY_LOG=0 python batch_test.py <version> <seeds>
```

`batch_test.py` 中 `SPEED=100`。

### 6.1 固定种子结果

| Version | Avg Score | Orders | Value | Drop | Collision | Overtime |
|---|---:|---:|---:|---:|---:|---:|
| V3 | 1757.8 | 22.2 | 2552 | 1008 | 1723 | 79.2 |
| V4 final | 2508.1 | 22.2 | 2524 | 1016 | 948 | 83.9 |

V4 相比 V3：

$$
\Delta Score = 2508.1 - 1757.8 = 750.3
$$

主要收益来自碰撞扣分下降：

$$
\Delta P_{collision} = 948 - 1723 = -775
$$

订单数基本持平，说明本轮优化没有明显牺牲吞吐。

### 6.2 固定种子分项

| Seed | V3 Score | V4 Score | 提升 |
|---:|---:|---:|---:|
| 100 | 1154.3 | 2149.3 | +995.0 |
| 252 | 1785.8 | 2980.4 | +1194.6 |
| 216 | 1745.2 | 2373.1 | +627.9 |
| 628 | 2752.1 | 2728.6 | -23.5 |
| 987 | 1351.5 | 2309.2 | +957.7 |

V4 在 5 个固定种子中有 4 个明显高于 V3，只有 seed=628 略低。

### 6.3 随机泛化结果

| Version | Avg Score | Orders | Value | Drop | Collision | Overtime |
|---|---:|---:|---:|---:|---:|---:|
| V3 | 2717.9 | 26.7 | 3058 | 1172 | 1476 | 36.3 |
| V4 final | 3153.3 | 26.5 | 3022 | 1165 | 1004 | 29.2 |

随机种子上 V4 平均分满足：

$$
3153.3 > 2500
$$

且高于同 seed 的 V3：

$$
3153.3 - 2717.9 = 435.4
$$

说明当前策略不是只对固定种子过拟合。

## 7. 结论

本轮 V4 的核心改进不是增加更多绕路，而是通过日志发现并修复清场振荡。最终策略保持合法道路约束，默认只启用低扰动 idle 清场，并用更长清场冷却与目标预留减少车辆在热点节点之间来回摆动。

最终结果满足两个关键要求：

- 固定种子 V4 平均分明显高于 V3，提升约 `750` 分。
- 随机种子 V4 平均分 `3153.3`，高于 `2500`，且高于同 seed V3。

虽然固定种子平均分尚未稳定达到 3000+，但在合法道路约束下，当前版本已经显著降低碰撞，并保持了与 V3 接近的订单吞吐。
