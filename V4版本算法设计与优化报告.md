# V4 版本算法设计与优化报告

## 1. V4 要解决什么问题？

本项目的最终目标是在 300s 仿真时间内尽可能提高总分。评分可以概括为：

$$
Score = R_{order} + R_{drop} - P_{collision} - P_{overtime}
$$

其中：

- $R_{order}$：完成成品订单获得的收益；
- $R_{drop}$：把原料投递到加工区获得的投料收益；
- $P_{collision}$：车辆距离过近造成的碰撞扣分；
- $P_{overtime}$：订单超时产生的扣分。

V3 版本已经能较好地完成“取原料-送加工区-取成品-送消费者”的任务链，但在可视化仿真中可以看到两个明显问题：

1. 多辆车容易在原料点、加工点、窄路交汇处聚集；
2. 某些车辆会因为暂时没有合适任务而停在路中或目标点附近，后续车辆经过时持续发生碰撞。

因此，V4 的核心思路不是单纯追求更短路径，而是在任务调度之外加入“交通管理”能力：当车辆之间出现危险接近时，用尽量小的干预让低价值车辆让出道路，从而减少碰撞扣分，同时尽量不破坏 V3 已经形成的运输吞吐。

## 2. 地图与路径的基本概念

### 2.1 节点与边

虽然前端背景图中绿色区域像“草坪”、灰色区域像“道路”，但策略真正可用的道路并不是图片像素，而是 `map.json` 中定义的图结构：

- 节点 $V$：车辆可以经过或停靠的位置；
- 边 $E$：两个节点之间允许通行的道路；
- 合法路径：连续节点之间必须存在边。

因此一条路径：

$$
P=(n_0,n_1,\ldots,n_k)
$$

必须满足：

$$
(n_i,n_{i+1}) \in E,\quad i=0,1,\ldots,k-1
$$

这也是 V4 的一个基础约束：任何避让、清场、重规划都不能让车辆穿过草坪，只能沿图中的合法边移动。

### 2.2 合法锚点

早期 V4 出现过一个问题：车辆正在道路中间移动时，如果直接从“最近节点”开始重规划，服务端会让车辆沿直线连到新的路径点，视觉上就可能穿过草坪。

修复后的思想是“先回到当前道路，再重新规划”。如果车辆正在从节点 $a$ 前往节点 $b$，那么新的路径不能从任意最近节点开始，而要先继续走到当前预览路径中的下一个锚点：

$$
Path=[p_{current},p_{anchor}] \oplus Dijkstra(n_{anchor},n_{target};E)
$$

其中：

- $p_{current}$ 是车辆当前位置；
- $p_{anchor}$ 是当前道路上的下一个合法节点位置；
- $Dijkstra$ 是在合法道路图上规划出的后续路径。

这样做保证了 V4 的所有覆盖路径仍然符合地图道路约束。

## 3. V4 的总体策略框架

V4 可以理解为在 V3 任务调度之上叠加了一层“低扰动避让控制”。

整体流程如下：

1. 先沿用 V3 的任务分配逻辑，决定车辆应该取原料、送原料、取成品还是送成品；
2. 生成基础运输路径；
3. 检查车辆之间是否存在危险接近；
4. 如果有危险接近，优先让低价值 idle 车辆进行短距离清场；
5. 若没有必要，则不干预原任务，避免频繁重规划破坏运输节奏。

这里的关键原则是“低扰动”：只有当碰撞风险已经比较明确时才干预；干预时优先选择对收益影响最小的车辆；干预路径尽量短，只移动到附近合法节点，而不是大幅改变任务路线。

## 4. 核心策略一：合法 idle 清场

### 4.1 为什么选择 idle 车辆清场？

车辆可以分为两类：

- moving 车辆：正在执行任务，例如赶往原料点、加工区或消费者；
- idle 车辆：当前没有路径，可能停在道路中、目标区附近或刚完成交互。

如果对 moving 车辆频繁重规划，可能导致它绕远路、错过订单时间，甚至降低整体吞吐。相反，idle 车辆通常没有正在执行的高价值任务，让它短距离挪开，代价更低。

因此 V4 在危险接近时优先选择 idle 车辆清场。

### 4.2 危险距离判断

当两车距离满足：

$$
d(v_i,v_j)<2.0
$$

就认为已经进入危险范围。此时如果其中有 idle 车辆，V4 会尝试让其中一辆车移动到附近的合法邻接节点。

### 4.3 清场目标如何选择？

清场不是随便找一个点，而是要同时考虑合法性和拥堵风险。候选目标必须满足：

- 是当前节点的合法邻接节点；
- 不与其它车辆当前位置重合；
- 不与本 tick 中其它清场车辆的目标冲突；
- 尽量避开正在被任务占用的目标节点；
- 对近期反复发生碰撞的热点节点进行降权。

可以把清场目标选择理解为：

$$
n^*=\arg\max_{n \in Adj(n_0)} ClearanceScore(n)
$$

其中 $Adj(n_0)$ 表示当前节点的邻接节点，$ClearanceScore$ 综合考虑与其它车辆的距离、是否已被预留、是否为热点等因素。

## 5. 核心策略二：防止清场振荡

日志复盘中发现，若只要接近就清场，车辆可能会在两个相邻节点之间来回移动：

$$
n_a \rightarrow n_b \rightarrow n_a \rightarrow \cdots
$$

这种现象称为“清场振荡”。它虽然暂时降低了某一刻的碰撞风险，但会不断打断车辆后续任务，使车辆反复处于无效移动状态。

V4 使用两个机制缓解振荡：

1. 清场冷却：同一车辆被清场后，在一段时间内不再被重复清场；
2. 节点预留：同一个 tick 内，已经被选为清场目标的节点不再分配给其它车辆。

最终采用的清场冷却时间为：

$$
T_{clearance}=2.5s
$$

这使得清场动作更像“必要时让一下”，而不是频繁来回摆动。

## 6. 核心策略三：清场速度调优

本轮优化中最稳定有效的参数调整是提高清场速度。

原策略中清场速度为：

$$
v_{clear}=12.0
$$

测试后最终改为：

$$
v_{clear}=16.0
$$

原因是：清场车辆如果移动太慢，会在碰撞半径附近停留更久，后车仍可能继续扣分；但速度过高又可能把车辆快速送入新的拥堵区域。`16.0` 是在固定种子和随机种子上都相对稳定的折中值，低于服务端最高速度 `20`。

## 7. 关于预调度的尝试与取舍

用户观察到有些车辆会因为暂时没有任务而停在半路。直觉上，可以让车辆提前前往“可能马上产生需求”的地点，这就是预调度思想。

本轮实际尝试了几类预调度：

1. 空车提前靠近原料点或加工点；
2. 载货车提前靠近未来可能需要该原料的加工区；
3. 对同向跟车进行降速错峰；
4. 对当前订单暂时不需要的原料尝试丢弃，让车辆重新参与调度；
5. 对原料区库存为 2 的情况，尝试并行派车预取。

日志和批测显示，这些方法都存在一个共同风险：车辆会提前集中到未来需求点附近，但该地图道路较窄，提前集中反而会制造新的碰撞热点。尤其是加工区入口附近，一旦多辆车提前排队，清场动作会变得更频繁，订单吞吐反而下降。

因此最终 V4 采取保守结论：

- 预调度相关能力保留为实验开关；
- 默认版本不启用激进预调度；
- 当前最终策略仍以“合法清场 + 防振荡 + 清场速度调优”为主。

对应默认开关为：

```text
V4_PREDISPATCH=0
V4_PARALLEL_RAW_PICK=0
V4_ABANDON_STALE_RAW=0
V4_SAME_DIRECTION_STAGGER=0
```

这个结论也说明：在多智能体运输中，“提前出发”不一定总是好策略。若地图容量有限，过早聚集会把时间收益转化为拥堵损失。

## 8. 最终关键参数

| 参数 | 默认值 | 含义 |
|---|---:|---|
| `COLLISION_WARN_DISTANCE` | 3.5 | 近距离检测半径 |
| `IDLE_CLEAR_DISTANCE` | 2.0 | 触发紧急清场的危险距离 |
| `CLEARANCE_SPEED` | 16.0 | 清场移动速度 |
| `CLEARANCE_COOLDOWN_SECONDS` | 2.5 | 同一车辆重复清场的冷却时间 |
| `REPLAN_COOLDOWN_SECONDS` | 1.0 | 可选 moving 重规划冷却 |
| `V4_MOVING_REPLAN` | 0 | 默认不启用 moving 绕路 |

## 9. 实验结果

批测命令格式：

```bash
STRATEGY_LOG=0 python batch_test.py <version> <seeds>
```

仿真速度设置为：

```text
SPEED = 100
```

### 9.1 固定种子结果

固定种子：

```text
100, 252, 216, 628, 987
```

| Version | Avg Score | Orders | Value | Drop | Collision | Overtime |
|---|---:|---:|---:|---:|---:|---:|
| V3 | 1704.9 | 22.0 | 2525 | 1012 | 1752 | 80.1 |
| V4 final | 2661.5 | 22.8 | 2613 | 1036 | 917 | 70.5 |

固定种子上，V4 相比 V3：

$$
\Delta Score = 2661.5 - 1704.9 = 956.6
$$

碰撞扣分下降：

$$
\Delta P_{collision}=917-1752=-835
$$

这说明 V4 的收益主要来自降低碰撞，同时订单吞吐没有明显下降。

### 9.2 随机泛化结果

随机泛化种子：

```text
37, 149, 314, 512, 777, 901
```

| Version | Avg Score | Orders | Value | Drop | Collision | Overtime |
|---|---:|---:|---:|---:|---:|---:|
| V3 | 2750.7 | 26.8 | 3066 | 1170 | 1453 | 31.8 |
| V4 final | 3302.7 | 26.7 | 3049 | 1167 | 878 | 34.8 |

随机种子上：

$$
3302.7 > 2500
$$

且 V4 高于 V3：

$$
\Delta Score = 3302.7 - 2750.7 = 552.0
$$

这说明 V4 的改进不是只针对单一 seed，而是在随机样本上也有较好的泛化表现。

## 10. 总结

V4 的设计重点可以概括为三句话：

1. 路径必须合法：所有重规划和清场都严格沿 `map.json` 的道路边移动；
2. 避让必须低扰动：优先让 idle 车辆短距离清场，而不是频繁改变执行任务车辆的路线；
3. 预调度必须谨慎：提前靠近需求点有潜在收益，但在窄路地图中容易形成新拥堵，因此最终默认不启用激进预调度。

最终 V4 在固定种子和随机种子上都明显高于 V3，主要提升来自碰撞扣分的大幅下降，同时保持了接近 V3 的订单吞吐能力。
