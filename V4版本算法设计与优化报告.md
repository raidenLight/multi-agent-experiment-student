# V4 版本算法设计与优化报告

## 1. 背景与优化目标

本项目的核心任务是在 300 秒仿真时间内，调度 10 辆无人车在共享道路网络上完成“原料运输、加工生产、成品配送”这一供应链流程，并尽可能获得更高总分。评分函数可以概括为：

$$
Score = R_{order} + R_{drop} - P_{collision} - P_{overtime}
$$

其中，$R_{order}$ 表示订单完成收益，$R_{drop}$ 表示原料投递奖励，$P_{collision}$ 表示碰撞扣分，$P_{overtime}$ 表示订单超时扣分。

V1 到 V3 主要解决的是任务调度问题，包括目标占用、订单优先级、需求感知补料等。V3 的特点是吞吐量较高，车辆会积极取料、投料和送单，但由于多辆车共享道路和目标点，在高密度运行时容易出现以下问题：

- 多辆车同时到达同一个加工区或原料区，停在同一个节点附近，持续触发碰撞。
- 两辆车在窄路或同一路段相向行驶，距离过近时来不及避让。
- 某些车辆已经完成交互或失去任务价值，但仍停留在交通热点，阻塞后续车辆。
- 如果避碰策略过度保守，又会导致车辆频繁绕路、清场或等待，从而降低订单吞吐。

因此，V4 的优化目标不是单纯“减少碰撞”，而是在保持 V3 高任务吞吐的基础上，尽量降低最严重、最持续的碰撞扣分。换句话说，V4 要解决的是：

$$
\max \left(R_{order} + R_{drop} - P_{collision} - P_{overtime}\right)
$$

而不是只优化某一个单项指标。

## 2. V3 暴露出的主要问题

在批量测试和日志分析中，V3 的平均订单完成数较高，但碰撞扣分也很高。例如在种子集合 `[1, 2, 3, 100, 231, 235]` 上，V3 的平均表现为：

| Version | Avg Score | Orders | Value | Drop | Collision | Overtime |
|---|---:|---:|---:|---:|---:|---:|
| V3 | 1791.4 | 22.3 | 2555 | 1013 | 1679 | 97.8 |

可以看到，V3 每局平均能完成 22.3 单，任务吞吐很强，但平均碰撞扣分达到 1679 分。由于一次碰撞会产生固定扣分，并且碰撞车辆有约 1 秒冷却时间，如果两辆车长期停在同一个点，碰撞会以近似周期性的方式不断累积。

这种持续碰撞通常不是普通路径最短路导致的短暂擦碰，而是由以下场景造成的：

1. **目标点堆叠**：加工区、原料区、成品区都是图上的固定节点，多辆车同时被调度到同一个节点后，很容易停在完全相同的位置。
2. **交互后滞留**：车辆完成 pick/drop 后，下一次任务分配可能仍然把它留在热点附近，或者新的车辆已经进入同一目标点。
3. **避让不及时**：如果只在距离较远时重规划，实际到达碰撞半径内后可能已经来不及错开。
4. **过度避碰的副作用**：早期 V4 曾尝试对所有近距离车辆做拥堵路径和目标占用控制，但结果表明这会显著降低订单完成数，得分反而下降。

因此，V4 的设计经历了从“最小侵入式避碰”到“默认稳定吞吐、可选合法避让”的调整：避让模块保留，但最终评分默认不主动覆盖路径。

## 3. V4 的总体设计思想

V4 的核心思想可以总结为：

> 保留 V3 的任务调度作为主策略；在需要实验避让时，通过可选模块插入合法的局部清场或重规划动作。

从控制结构上看，V4 可以分为两层：

1. **主任务层**：沿用 V3 的订单排序、目标 claim、需求感知补料等机制，决定车辆“应该做什么”。
2. **局部避碰层**：作为可选模块，在每个 tick 后处理车辆间距离，决定是否需要覆盖某些车辆的原命令，使其临时清场或降速。

这种结构的好处是，V4 不重新设计整个调度系统，而是作为一个安全修正层叠加在 V3 上。形式上可以理解为：

$$
Command_{V4}(v) =
\begin{cases}
Override_{avoid}(v), & \text{if } v \text{ is in an emergency conflict} \\\\
Command_{V3}(v), & \text{otherwise}
\end{cases}
$$

也就是说，在开启 `V4_AVOIDANCE=1` 时，只有当车辆进入明确的危险状态时，V4 才覆盖 V3 的原始命令；默认评分配置下不启用该覆盖层。

## 4. 关键策略一：确定性重规划与可控随机性

早期版本中曾保留一定概率的随机重规划：

$$
P_{random} = 0.15
$$

随机重规划的优点是有机会跳出局部拥堵，但缺点是批测波动较大，不利于课程评测和复现实验。最终 V4 将随机性改为默认关闭，只有设置：

```bash
STRATEGY_RANDOM_REPLAN=1
```

时才启用随机选择。

默认情况下，V4 采用确定性选择规则：

1. 优先保留收益更高的车辆。
2. 优先保留正在执行高价值成品任务或携带成品的车辆。
3. 优先让低收益、空车或低价值原料车避让。
4. 若收益接近，则按车辆编号稳定打破平局。

车辆任务价值用一个简单的启发式函数估计：

$$
Gain(v) = Reward(task_v) - \lambda \cdot Dist(v, target_v)
$$

其中，$\lambda = 0.03$，表示距离成本权重。任务收益按任务类型估计：

- 成品相关任务：使用对应订单产品价值，例如 B1 为 150，B5 为 90。
- 原料相关任务：默认收益估计为 30。
- 无活动任务车辆：收益为 0。

这个估计不追求全局最优，只用于局部冲突时判断“谁更值得保留当前任务”。

## 5. 关键策略二：同点紧急分离（可选模块）

V4 避让模块中最重要的改动是“同点紧急分离”。碰撞判定由服务端完成，车辆半径为 1 米，两车距离小于 2 米时发生碰撞。因此 V4 将危险距离设为：

$$
d(v_i, v_j) < 2.0
$$

当两辆车进入该距离阈值时，可选避让模块不再依赖普通路径重规划，而是立即触发 emergency separation。

### 5.1 冲突组识别

开启 `V4_AVOIDANCE=1` 后，V4 会在每个 tick 检查所有车辆对：

$$
\forall (v_i, v_j),\quad d_{ij} = \lVert p_i - p_j \rVert_2
$$

若 $d_{ij} < 2.0$，则认为这两辆车属于同一个危险冲突组。如果多个车辆两两相连，则合并为一个冲突组：

$$
G = \{v_1, v_2, ..., v_k\}
$$

这样可以处理三辆或更多车辆同时堆在同一目标点的情况。

### 5.2 保留车辆选择

对于每个冲突组，V4 会选择一辆车作为 keeper，其余满足清场条件的车辆执行临时清场动作。keeper 的选择依据包括：

- 当前任务预期收益 $Gain(v)$。
- 是否正在移动。
- 是否携带成品或原料。
- 是否执行成品投递、成品取货等高价值任务。

可以抽象为：

$$
Keeper = \arg\max_{v \in G}
\left(Gain(v) + B_{status}(v) + B_{cargo}(v) + B_{task}(v)\right)
$$

其中：

- $B_{status}$：移动车辆有额外加分，避免高价值在途任务被频繁打断。
- $B_{cargo}$：携带物品车辆优先级高于空车，携带成品优先级高于携带原料。
- $B_{task}$：成品相关任务额外加分。

### 5.3 清场车辆选择安全邻居节点

被清场车辆不会重新选择完整任务，而是临时移动到当前节点的一个安全邻居节点。安全邻居的选择原则是：

1. 不能是其它车辆当前占用的节点。
2. 不能是本 tick 已经为其它清场车辆预留的节点。
3. 尽量选择离其它车辆最远的邻居。

若当前节点为 $n$，其邻接节点集合为 $Adj(n)$，则清场目标可以表示为：

$$
n^* =
\arg\max_{u \in Adj(n)}
\min_{v_j \neq v_i} \lVert pos(u) - p_j \rVert_2
$$

清场命令的 action 设置为 `None`，仅发送短路径和速度：

```python
path = [current_node, safe_neighbor_node]
action = None
speed = CLEARANCE_SPEED
```

这样做的含义是：车辆只是临时让出位置，不改变原服务端协议，也不主动执行 pick/drop。

## 6. 路径合法性修复：避免重规划穿越草坪

后续可视化复盘发现，早期 V4 在近距离重规划或清场时可能出现“车辆直接穿过绿色草坪驶向下一个节点”的现象。虽然评分代码没有对该行为额外扣分，但从地图语义上看这是不合法的：车辆只能沿道路网络行驶。

本项目中合法道路的来源不是 `frontend/data.png` 这张背景图，而是图结构本身：

- `map.json` 中的 `edges` 是唯一合法道路定义。
- `server/game_engine.py` 初始化地图时将 `edges` 加载为 `state.adjacency`，即服务端使用的无向邻接表。
- `sdk/agent_sdk.py` 接收 `graph_info["edges"]` 后，将同一批边加载为 `StrategySDK._adjacency`。

因此，V4 生成的任意路径都应满足：

$$
(n_i,n_{i+1}) \in E,\quad i=0,1,\dots,k-1
$$

其中 $E$ 是 `map.json` 中定义的边集合。

### 6.1 bug 成因

服务端执行路径时，不会检查路径是否沿道路边行驶，而是调用 `Vehicle.set_path()` 在命令 path 中寻找离车辆当前位置最近的线段，然后让车辆沿该线段终点方向直线移动。因此，如果 V4 在车辆位于道路中点时直接发送：

$$
[nearest\_node,\ next\_target,\dots]
$$

服务端可能会把车辆当前位置投影到一条新的直线段上，导致车辆从道路中点切向另一个节点。视觉上看，就是车辆穿过草坪。

### 6.2 合法锚点规划

修复后的 V4 将“重规划起点”改为合法锚点：

1. 若车辆正在 moving，则新的 path 必须先从当前位置继续到当前道路上的下一路径点，即 `path_preview[0]`。
2. 只有到达该锚点后，才允许从对应图节点重新执行 Dijkstra。
3. 若车辆 idle，则从当前位置对应图节点开始规划。
4. 除首段 `current_position -> anchor_node` 外，后续所有节点对都必须存在于 `_adjacency`。

可以表示为：

$$
Path =
[p_{current},\ p_{anchor}]
\oplus
Dijkstra(n_{anchor}, n_{target}; E)
$$

并额外校验：

$$
\forall (n_i,n_{i+1}) \in Path_{nodes},\quad n_{i+1}\in Adj(n_i)
$$

如果无法构造合法路径，V4 会放弃覆盖命令，而不是生成一条可能穿越草坪的捷径。

### 6.3 对清场与重规划的影响

这次修复覆盖了所有 V4 可能主动生成新 path 的入口：

- 清场命令不再直接使用“最近节点到安全邻居”的直线段，而是先锚定到当前道路下一节点。
- 普通重规划从合法锚点开始，而不是从当前位置最近节点开始。
- 速度错峰继续沿原 `path_preview` 行驶，不制造新的几何捷径。
- 拥堵感知路径如果开启，也使用同一套合法路径拼接逻辑。

这意味着：即使开启 V4 的避让实验开关，车辆也只会沿 `map.json` 的边行驶。

## 7. 可选避让模块：默认关闭普通路径覆盖

在修复路径合法性后，我们重新测试了 V4 的避让覆盖逻辑。结果显示：原来一部分高分来自非法捷径带来的隐性收益；当车辆必须严格沿道路边行驶后，频繁的 moving 重规划和 emergency clearance 会显著打断任务吞吐。

因此，最终 V4 采用了更保守的默认配置：

```bash
V4_AVOIDANCE=0
```

默认情况下，V4 不再覆盖 moving/idle 车辆路径，而是保留 V3 的主任务调度吞吐。已经修复的合法避让模块仍保留在代码中，用于可视化演示和后续实验：

```bash
V4_AVOIDANCE=1
```

若进一步希望开启普通 moving 重规划，还需要显式设置：

```bash
V4_MOVING_REPLAN=1
```

这一设计的含义是：最终交付版本优先保证“合法行驶 + 稳定吞吐”；避让重规划作为可选实验功能保留，但不作为默认评分策略。

## 8. 保留但默认关闭的拥堵路径与目标占用

在调试过程中，我们还尝试了两个更激进的策略。

### 8.1 拥堵感知路径

拥堵感知路径会在普通任务寻路时，对其它车辆当前节点、下一节点和未来路径节点增加惩罚：

$$
Cost'(u, v) = Cost(u, v) + Penalty(v)
$$

使用过的主要惩罚参数包括：

| 参数 | 含义 | 数值 |
|---|---|---:|
| `CONGESTION_CURRENT_PENALTY` | 其它车辆当前节点惩罚 | 60 |
| `CONGESTION_NEXT_PENALTY` | 其它车辆下一节点惩罚 | 35 |
| `CONGESTION_FUTURE_PENALTY` | 未来路径节点惩罚 | 18 |
| `CONGESTION_TARGET_PENALTY` | 已有任务目标节点惩罚 | 25 |

这个策略理论上可以减少路上相遇，但实际测试发现它会让大量车辆绕路，降低订单吞吐。在当前任务中，订单收益远高于一次短暂绕行收益，因此最终将其默认关闭，仅保留环境变量开关：

```bash
V4_CONGESTION_PATH=1
```

### 8.2 物理目标占用

另一个尝试是：若加工区附近已经有 idle 车辆，则暂不派其它车辆前往同一加工区。这个策略可以减少目标点堆叠，但如果限制过严，会导致原料投递和成品生产变慢。早期测试中曾出现订单数大幅下降的情况。

因此最终也将其默认关闭，仅作为可选调试开关：

```bash
V4_PHYSICAL_ZONE_OCCUPANCY=1
```

这两个策略的调试结论是：在本项目评分结构下，不能为了减少碰撞过度牺牲吞吐。最终 V4 的有效版本是“默认不重写路径，必要时再显式开启合法避让实验”。

## 9. 参数设置与调优结论

最终 V4 默认使用的关键参数如下：

| 参数 | 数值 | 作用 |
|---|---:|---|
| `IDLE_CLEAR_DISTANCE` | 2.0 | 真实危险距离，低于该距离触发紧急分离 |
| `COLLISION_WARN_DISTANCE` | 3.5 | 可选避让模块的近距离预警半径 |
| `CLEARANCE_SPEED` | 12.0 | 可选清场移动速度 |
| `REPLAN_COOLDOWN_SECONDS` | 0.5 | 可选重规划冷却时间 |
| `EXPECTED_GAIN_WEIGHT` | 0.03 | 任务距离成本权重 |
| `RANDOM_REPLAN_PROB` | 0.15 | 随机重规划概率，仅在开启随机模式时生效 |
| `V4_AVOIDANCE` | 0 | 默认关闭路径覆盖避让 |
| `V4_MOVING_REPLAN` | 0 | 默认关闭 moving 普通重规划 |

调参过程中的主要发现：

1. 合法路径修复后，避让路径不再能通过“草坪捷径”快速脱困，清场和重规划的真实代价上升。
2. `CLEARANCE_SPEED=16`、`REPLAN_COOLDOWN_SECONDS=0.5`、`COLLISION_WARN_DISTANCE=3.5` 在一次网格测试中平均分最高，达到 2596.3，但复测波动很大，不适合作为默认。
3. `CLEARANCE_SPEED=20` 过于激进，多个组合出现订单吞吐下降和超时上升。
4. 只清 idle 车辆虽然减少了对 moving 任务的打断，但在 `speed=100` 下仍会造成部分 seed 任务节奏恶化。
5. 最终默认关闭 V4 避让覆盖，保留合法避让代码作为可选开关；默认策略回到稳定吞吐优先。

参数探索中的关键结果如下：

| 配置 | Avg Score | Orders | Collision | 结论 |
|---|---:|---:|---:|---|
| 合法修复后保守避让 `cs=12, cd=1.0, warn=3.5` | 2390.3 | 22.0 | 1020 | 单次表现较好，但复测不稳定 |
| 网格最高 `cs=16, cd=0.5, warn=3.5` | 2596.3 | 22.5 | 922 | 峰值最高，但复测均分降至 670.6 |
| 最高速度 `cs=20, cd=0.5, warn=3.5` | 513.7 | 11.5 | 1201 | 过度激进，吞吐崩溃 |
| idle-only 清场 | 616.9 | 11.3 | 1035 | 仍会打断任务节奏 |
| 最终默认 `V4_AVOIDANCE=0` | 2361.8 | 25.2 | 1550 | 稳定吞吐，最终采用 |

## 10. 实验结果对比

根据后续复测要求，本节重新使用种子 `[123, 234, 345, 456, 567, 678]` 进行批量测试，并将仿真加速倍率设置为 `speed=100`。选择较低加速倍率的原因是：当 `speed` 过高时，服务端与策略端之间的状态更新、命令发送和碰撞检测更容易受到执行时序影响，可能放大偶然波动。因此，本组结果更适合作为稳定对比。

批测命令形式如下：

```bash
STRATEGY_LOG=0 python batch_test.py student_v4 123 234 345 456 567 678
```

其中 `batch_test.py` 中的 `SPEED` 设置为 `100`。

### 10.1 汇总结果

| Version | Avg Score | Orders | Value | Drop | Collision | Overtime |
|---|---:|---:|---:|---:|---:|---:|
| V3 | 2257.3 | 24.5 | 2770 | 1098 | 1558 | 52.7 |
| V4 final | 2361.8 | 25.2 | 2848 | 1115 | 1550 | 50.7 |

### 10.2 分种子结果

| Seed | V3 Score | V4 Final Score | V4 相对 V3 |
|---:|---:|---:|---:|
| 123 | 2398.9 | 2223.6 | -175.3 |
| 234 | 2413.1 | 3149.0 | +735.9 |
| 345 | 598.9 | 789.7 | +190.8 |
| 456 | 2197.4 | 2182.4 | -15.0 |
| 567 | 3162.0 | 3332.6 | +170.6 |
| 678 | 2773.3 | 2493.3 | -280.0 |

从最终结果看，V4 final 在 6 个种子中的 3 个种子高于 V3，整体平均分略高。提升主要不是来自大幅降低碰撞，而是来自保持高吞吐的同时略微提高订单收益和投料收益。由于默认关闭避让覆盖，V4 final 更接近“带合法避让可选模块的 V3 稳定版”。

### 10.3 指标分析

从平均指标看，V4 相比 V3 的变化为：

$$
\Delta Score = 2361.8 - 2257.3 = 104.5
$$

平均分提升约 104.5 分。具体拆分如下：

$$
\Delta R_{order} = 2848 - 2770 = 78
$$

$$
\Delta R_{drop} = 1115 - 1098 = 17
$$

$$
\Delta P_{collision} = 1550 - 1558 = -8
$$

$$
\Delta P_{overtime} = 50.7 - 52.7 = -2.0
$$

也就是说，最终 V4 的默认版本没有再通过大规模重规划显著降低碰撞，而是通过保留稳定吞吐获得略高总分。更重要的是，它修复了可视化中不合法的穿草坪路径问题，并将激进避让作为可选模块保留。

可以用评分函数解释这一点：

$$
\Delta Score
= \Delta R_{order}
+ \Delta R_{drop}
- \Delta P_{collision}
- \Delta P_{overtime}
$$

代入平均值近似为：

$$
\Delta Score
\approx 78 + 17 - (-8) - (-2)
\approx 105
$$

这与实际平均分提升 104.5 基本一致。

### 10.4 结论

在 `speed=100`、种子 `[123, 234, 345, 456, 567, 678]` 的最终复测条件下，V4 final 平均分略高于 V3。更关键的是，V4 final 不再依赖非法草坪捷径；所有可选避让路径都经过合法锚点与边校验。

## 11. 总结

V4 的核心贡献经历了两次调整。

第一阶段，V4 尝试把“路径协同”作为局部安全修正层叠加在 V3 上，通过同点分离、低收益车辆让路和近距离重规划降低碰撞。这个方向在不考虑可视化道路合法性时能获得较高分数，但可视化复盘发现部分重规划路径会穿越草坪。

第二阶段，V4 修复了路径合法性问题：所有可选重规划和清场路径必须先锚定到当前道路下一节点，再沿 `map.json` 的 `edges` 规划。修复后，激进避让的真实路径代价上升，在 `speed=100` 批测中稳定性不足。因此最终默认关闭避让覆盖，保留合法避让模块作为可选实验功能。

最终 V4 的设计可以概括为三点：

1. **合法道路优先**：车辆路径以 `map.json edges` 为唯一可行道路定义，不再生成穿草坪捷径。
2. **高吞吐优先**：默认保留 V3 的任务分配与最短路导航，不轻易打断车辆任务。
3. **避让可选开启**：需要可视化展示或继续实验时，可用 `V4_AVOIDANCE=1` 开启已修复的合法避让模块。

这个设计体现了多智能体协同运输中的一个重要经验：避碰策略不能脱离地图约束和评分函数单独优化。对于本项目，非法捷径带来的高分不应作为算法收益；在合法道路约束下，稳定吞吐比频繁路径覆盖更可靠。
